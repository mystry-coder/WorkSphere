# WorkSphere

worksphere/
├── client/              (React app — everyone's UI lives here)
│   └── src/
│       ├── pages/
│       ├── components/
│       └── api/         (axios calls to backend)
├── server/              (Express app)
│   ├── src/
│   │   ├── routes/
│   │   ├── controllers/
│   │   └── middleware/
│   └── prisma/
│       └── schema.prisma
└── README.md
